9/23:

Super Kitties

Figured out how to debug the nonce generation
  - Can use Ctrl+P in debugger to search for files by name
  - Use this to open pre-init.js?v2 (which I can see is source of network traffic)
  - Placed breakpoint in genNonce(), tried using the nonce before the browser returned it
    - Does not work
    - So the browser must be communicating the nonce to the server somehow...
    - Maybe I can debug through and see at what point it does this?
      - It definitely calls beacon()
        nonce (maybe whatever we want?), status ("clear"), videoId (from pid in index.php url)
      - e.g. '{"nonce":"0eaf0c2328c3a35efb0cd6c05edefa0b","status":"clear","id":"993784"}'
      - it's sending to ad-verify

The beacon is sending and getting a 204
  - But the script still isn't happy
  - If I load the webpage but prevent it from sending its beacon...
    - and send my beacon instead, it says 204
    - the browser gets the 403
  - If I load webpage but then send my beacon with the already ok nonce...
    - then my beacon fails with 400, but the download works
  - So I don't invalidate the existing approval
    - but I can't get approval myself
      - am I sending incorrectly? Is there something else needed?

Sending the webpage beacon with cURL and my nonce doesn't work
  - Load the page but prevent it from sending beacon
  - Send their nonce in the cURL from my terminal?
    - This worked
  - Try it again, but send the beacon from python
    - If this doesn't work, problem is how python sends beacon
    - If this DOES work, then it's something before the beacon
      - Is the page requesting additional ads or something?

If I stop the page when nonce is first generated and send with cURL
  - doesn't work

If I stop the page before beacon is sent and send with cURL instead
  - works!

So something happens between nonce generation and beacon to make server happy
  - validate with cURL beacons for now, since I know those can work
  - try to figure out the difference beween cURL and python beacons after

9/22:

Super Kitties

Trying www.wcostream.tv for kicks, since it redirected me to that
  - maybe these techniques will work on older site, too
  - not sure there's any advantage, but this is where I started work

video-old-js.php gives me HTML with <script> that contains the URL I need
  - Can I get the <script> block and then regex for the $.getJSON part?
  - or should I just regex teh whole page?
  - right now my regex is struggling...

Can download individual URLs now
  - Need to grab nonce manually from browser, though
    - How is this being validated?
    - Only nonces generated by the page I'm trying to get to seem to work...
      - Is this nonce being associated with this link by the first video-js-old.php call?
      - Why can't I create my own nonces?
      - Tried generating from browser myself, still not valid
  
  - Looks like there's a beacon of some sort in the code
    - Is this being sent in network history? I didn't see nonce anywhere else in HAR...
    - Maybe beacon isn't being used here
    - Is something else happening in a different web call that is paving the way?
      - Maybe the nonce is a red herring
      - Try loading page with random nonce after it's been loaded in browser?
